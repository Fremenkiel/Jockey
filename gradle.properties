pluginGroup=jockey
pluginVersion=1.0.0

# Rider build to compile against. 262 = Rider 2026.2.
platformVersion=2026.2.2
pluginSinceBuild=262

# If this folder exists, the build uses the local Rider install instead of downloading the SDK.
platformLocalPath=/Applications/Rider.app

kotlin.stdlib.default.dependency=false
org.gradle.configuration-cache=true
org.gradle.caching=true
